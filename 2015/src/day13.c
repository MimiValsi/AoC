#include "misc.h"
#include <stdio.h>

int part1(FILE *file);
int part2(FILE *file);

int main()
{
	FILE *file = fopen("./data/day13.txt", "r");
	int p1 = part1(file);
	printf("p1: %d\n", p1);
	// rewind(file);
	// int p2 = part2(file);
	// printf("p2: %d\n", p2);
	return 0;
}

int part1(FILE *file)
{
	int total = 0;

	return total;
}

int part2(FILE *file)
{
	int total = 0;

	return total;
}
